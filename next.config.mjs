/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'c10.patreonusercontent.com', 'lh3.googleusercontent.com'],
        deviceSizes: [320, 420, 768, 1024, 1200],
      },
};


export default nextConfig;